# ADR-004

## Title

Render Then Populate

## Status

Accepted

## Decision

Views are rendered before being populated with application data.

## Rationale

Rendering creates the DOM structure.

Population updates the rendered elements using `appState`.

This allows views to be safely re-rendered at any time without reloading data.